# A1 Handoff — 2026-05-15 (C1 → A1)

C1 has run consolidation + audit through the operator's 2026-05-15 directives. This doc collects what's pending on A1's side so the next A1 session resumes cold without re-tracing context.

## Priority 1 — Unblockers (do first)

### 1a. Configure Supabase Auth Redirect URLs (unblocks D0 PR #113)

Source: [bot_chat 145](#) (D0 → A1, 15:14 UTC). One-time operator-dashboard action — no code change.

Supabase → Auth → URL Configuration → Redirect URLs, add:
- `https://vibepass-terminal-test.onrender.com/static/terminal/login.html` (prod)
- `http://localhost:8000/static/terminal/login.html` (dev)

Google OAuth credentials need **no** change. The existing `https://hzrizjeaxlqcxfrtczpq.supabase.co/auth/v1/callback` already covers D2 and reuses for D0. After URLs land → reply to 145 with `status` event → C1 merges PR #113 in the next consolidation pass.

### 1b. Review + merge PR #114 (C1 omnibus)

Branch: `claude/agitated-visvesvaraya-27f782`. Head commit `4e117f6`. Contains:

- `docs/c1-checkpoint-2026-05-15.md` — first formal checkpoint
- `docs/audit-2026-05-15-consolidation-plan.md` — 6-cluster plan
- `docs/bot_chat_conventions.md`, `docs/current-state-index.md`, `docs/audit-2026-05-15-schema-sprawl.md`, `docs/audit-2026-05-15-migration-header-bloat.md`, `docs/audit-2026-05-15-merge-token-leakage.md`
- `docs/token-discipline-rules.md` — operator-assigned charter (bot_chat 139)
- `docs/A1_HANDOFF_2026-05-15.md` — this doc
- `LANE_DISCIPLINE.md` — C1 charter expansion line
- `docs/c1_daily_checkpoint_runbook.md` — Step 8 event_type fix + Step 9 token discipline
- `supabase/migrations/20260515340050_v_bot_chat_unresolved_status_close.sql` — Cluster D-1 view migration (rebumped from `…340000` to resolve collision with #118; awaits A1 apply window)

## Priority 2 — In-flight from A1's prior sign-off (bot_chat 138)

### 2a. Cluster B-1 silent-success investigation (bot_chat 131)

A1 committed to "TAKES B-1 NOW" in 138. Status unknown to C1; current `release_health_check()` shows listings staleness cleared, but bot_chat 131 has no `status` reply yet. Starter diagnostics:
- `cron_gate_decisions` around `collect-listings-0-24h` fires
- `cron.job_run_details.return_message` + duration for that job
- `pg_net._http_response` 4xx/5xx in that window
- `count(*) FROM listings_snapshots WHERE captured_at > '<window>'` delta

If listings is actually fixed (staleness was 261 min → 0 sometime today), please post `status` to 131 closing it.

### 2b. Cluster B-2 — 3 persistent cron failures

Still failing every 15 min as of this handoff (12 fails in last 90 min):
- `orphan_event_backfill_process_15min` — `query has no destination for result data` (low-pri carried since Open follow-up §3)
- `orphan_event_backfill_queue_15min` — FK violation on `evo_event_backfill_pending_tevo_event_id_fkey`
- `midnight-catchup-sweep` — statement timeout in `_sec_norm`

These also evade the `subhourly_jobs_silent_90min` check by virtue of actually firing and failing rather than going silent. Either patch or accept-and-document.

### 2c. Cluster A drift-detection infra

From bot_chat 138 sign-off, A1 to:
- Integrate `_health_check_pg_net_errors()` (already shipped in migration `20260515142450`) into `release_health_check()` as a `RETURN QUERY` append. **B1 actually shipped this in PR #118** (`supabase/migrations/20260515340000_security_release_health_aging_and_pgnet.sql`, merged `bebf85b5`) — awaiting apply window. Confirm B1's bundling is the intended path or coordinate.
- Fix `migration_drift_check()` semantics — currently returns 100+ rows with null `applied_at`/`has_codified` columns; meaning ambiguous.
- Decide on `event_type='checkpoint'` constraint: add to allowlist OR keep runbook patched to use `'status'` (current). Runbook documents the workaround in Step 8.
- Pre-merge advisor gate (hard fail on new ERROR, soft warn on WARN per 138 Q4).

### 2d. Enable `track_functions = 'pl'`

138 commitment: `ALTER SYSTEM SET track_functions = 'pl'; SELECT pg_reload_conf();` if MCP permits, otherwise operator escalation. Wait 7 days then rerun Cluster F (`docs/audit-2026-05-15-schema-sprawl.md` §"Static-analysis attempt"). Target rerun date: 2026-05-22.

## Priority 3 — Migration filename collisions (file-naming only, no correctness bug)

Apply order resolves at apply-time (Supabase records `now()` as version, not parsing filename prefix), so these aren't functional bugs. But `+30/+50 on collision` convention says rename before next apply window.

### 3a. `20260515300000` × 3 files

- `20260515300000_cron_resume_with_gate_trailing_semicolon_fix.sql` (PR #112 — already applied as DB version `20260515142436`)
- `20260515300000_security_secdef_post_pr101_compliance.sql` (PR #110 — not applied)
- `20260515300000_vivid_orders_aq_linkage.sql` (PR #116 — not applied)

Recommend: rename PR #110's → `20260515300050_…` before apply. Renaming after apply has no effect on schema state.

### 3b. `20260515320000` × 2 files

- `20260515320000_d0_read_access_grants.sql` (PR #115 — applied as DB version `20260515142444`)
- `20260515320000_d2_metrics_rpcs.sql` (PR #116 — not applied)

Recommend: rename PR #116's d2_metrics_rpcs → `20260515320050_…` before apply.

C1 already self-fixed the third collision (mine vs #118): `20260515340000_v_bot_chat_unresolved_status_close.sql` rebumped to `20260515340050_*` in commit `4e117f6` on PR #114.

## Priority 4 — Open follow-ups already tracked

These are in C1's daily checkpoint watch list (bot_chat 127); listed here for completeness:
- Render API key `rnd_WPA69TmT4vTttGvYa07GdMIqzfXz` rotation (still active — operator)
- `orphan_event_backfill_process_15min` destination bug (overlaps with 2b)
- 2 overnight backfills missed their windows on 2026-05-15: `listings_deltas_backfill_overnight` (04:30–44 UTC) + `sg_canonical_link_overnight` (04:46–59 UTC) — either reschedule or one-shot run
- `listings_aq_backfill_overnight` is `active=false` — confirm intentional or resume
- Open-Meteo archive retry (~2026-05-22)
- Operator xlsx re-extract for AQ Performer UUID
- Retire `master-cascade-2min` + `zone-backfill-isolated-10min` via `cron.unschedule()` per 138 Q5 — C1 verified docs reference these but no runtime dependency; safe to retire when ready

## Apply queue summary

| Migration | Author | Status | Notes |
|---|---|---|---|
| `20260515300000_security_secdef_post_pr101_compliance.sql` | B1 | not applied | Rename to `…300050` first; closes 7 ERROR RLS + 11 WARN SECDEF lints once applied |
| `20260515300000_vivid_orders_aq_linkage.sql` | D2 (#116) | not applied | Phase B columns on vivid_orders |
| `20260515320000_d2_metrics_rpcs.sql` | D2 (#116) | not applied | Rename to `…320050` first; D2 metrics RPCs |
| `20260515340000_security_release_health_aging_and_pgnet.sql` | B1 (#118) | not applied | Expands `release_health_check()` with 2 new categories |
| `20260515340050_v_bot_chat_unresolved_status_close.sql` | C1 (#114) | not applied | View change auto-excludes status-replied rows; safe; no data movement |

All 5 are codify-only — Supabase Preview integration is disabled, so merge doesn't auto-apply. Operator-authorized MCP `apply_migration` is the path.

## What C1 will do next

While A1 is offline:
1. Audit recently merged code (#117, #118) for additional token-leakage patterns beyond the spot-checks already in `docs/audit-2026-05-15-merge-token-leakage.md`.
2. Watch `release_health_check()` for any regressions from this morning's merges (no auto-apply but D1 storefront went to Render Starter plan — should observe latency change in metrics; non-blocking).
3. Skip work that blocks on A1 (Cluster A migrations, view apply, cron retires, migration renames).

## C1 contact surface

- C1 daily checkpoints: `docs/c1-checkpoint-<YYYY-MM-DD>.md`
- C1 conventions: `docs/bot_chat_conventions.md`, `docs/token-discipline-rules.md`
- C1 daily routine: `docs/c1_daily_checkpoint_runbook.md` (Steps 1–9)
- `bot_chat` `event_type='flag'` from `bot_lane='C1'` for new findings

End of handoff.
