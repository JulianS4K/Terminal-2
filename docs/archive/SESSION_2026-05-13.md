# Session 2026-05-13 — D2 (TickPick + Vivid) integration + scope tightening

Lane: D2 — TickPick + Vivid Order Clients (Data Collection, supervised by C1).

## PR shipped

| PR | Branch | Status |
|---|---|---|
| #56 | `claude/review-unified-order-suite-FA0qA` | merged |

What landed:
- `tickpick_client.py` (new) — read-only client for `api.tickpick.com/1.0/orders` + `/orders/:id/details`.
- `vivid_client.py` (new) — read-only client for `brokers.vividseats.com/webservices/v1` (`/getOrders`, `/getPendingRetransferOrders`, `/getOrder`). XML responses parsed to flat dicts.
- RULE 2 enforcement extended to both new hosts:
  - `scripts/check_readonly.py` — `FORBIDDEN_HOSTS` + `CLIENT_FILES` now include TickPick and Vivid.
  - `tests/test_readonly_guards.py` — 12 → 20 cases (8 new for TP/VIV).
  - `SCHEMA.md` — Layer 1 guard table extended to four clients.

Adjacent doc update (separate branch):
- `docs/bot-hierarchy.mermaid` on `claude/audit-datasets-schemas-auoc3` (commit `ab50a07`) — added D2 node, dotted feed edge `D2 -.->|unfulfilled / pending shipment orders| S3`, S3 annotated with the handoff branch.

## Vault reads (this session)

`public.get_app_secret(...)` called once for each:
- `TEVO_API_TOKEN`
- `TEVO_SECRET`
- `SEATGEEK_API_TOKEN`

One-time use; values not persisted to disk, env, or any DB row.

## DB reads (this session)

Tables: `evo_orders`, `evo_order_items`, `events`, `seatgeek_orders`, `seatgeek_sales_snapshots`, `seatgeek_event_xref`, `vault.secrets` (names only).

Views: `our_orders`, `unified_orders`, `v_event_sales_combined`, `v_sg_sales_by_event`.

No writes.

## External calls attempted

| Endpoint | Result |
|---|---|
| `GET api.ticketevolution.com/v9/orders?per_page=1&page=1` | `403 Forbidden — Host not in allowlist` |

Sandbox IP isn't on TEvo's allowlist. Live pulls continue to flow via Supabase pg_net + Railway workers as designed — this only affects ad-hoc calls from this session's container.

## Access controls now applied to this session

| Surface | State |
|---|---|
| Evo `GET /v9/orders` | **allowed** |
| Evo `GET /v9/orders/:id` | **allowed** |
| SG `GET /sales` | **allowed** |
| All other Evo endpoints | revoked |
| All other SG endpoints | revoked |
| All TickPick endpoints | revoked |
| All Vivid Seats endpoints | revoked |
| DB reads/writes | unchanged |
| Vault reads | unchanged in principle; secrets already fetched are not to be reused for any non-allowed endpoint |

This is a session-scope control, not a repo-wide RULE update. RULE 2 (read-only enforcement for all four hosts) continues to apply globally.

## Findings filed for other lanes (cross-link, not actioned by D2)

Coverage check on the orders/sales bucket:

| Source | Rows | xref'd | Coverage |
|---|---|---|---|
| `evo_order_items` | 87 | 87 in `events` | 100% |
| `seatgeek_orders` (our SG seller channel) | 400 | 0 with `tevo_event_id` | 0% |
| `seatgeek_sales_snapshots` (SG broker market data) | 1,787 | 365 with `tevo_event_id` | 20.4% |
| `seatgeek_event_xref` | 11 rows total | — | bottleneck |

Corrected by A1 (bot_chat row 62): the 0% SG seller figure was reading the wrong column. Path B resolution lives in the `seatgeek_orders_resolved` sidecar (400/400 populated; 271 venue-resolved, 166 venue+performer-resolved). Sidecar is canonical.

Items remaining:

1. **canonical lane / PR #51 phase 13** — `v_event_sales_combined` filters `tevo_event_id IS NOT NULL`, hiding Path B sales. Needs an addendum to UNION the sidecar.
2. **A1 lane / PR #66** — strict matcher (`auto_match_sg_canonical_relaxed`) hasn't backfilled `seatgeek_event_xref` against 425+ distinct `sg_event_id` values across orders + sales. Once it does, broker coverage rises from 20% naturally.
3. **D2 lane (this lane) policy** — sidecar-only. D2 will not write `seatgeek_orders.tevo_event_id` from ingest, and will follow the same pattern when TickPick/Vivid persistence ships (planned `tickpick_orders_resolved` / `vivid_orders_resolved` sidecars rather than in-row `tevo_event_id` writes).

## Pending follow-ups (D2)

- TickPick + Vivid DB persistence layer (tables + ingest cron + sidecars). Not in #56.
- S3 (Undelivered Front End Bot) consumes the clients once UI scope opens.
